# Docker configuration for URL Shortener

server:
  port: "8080"

database:
  host: "mariadb"
  port: "3306"
  user: "urlshortener"
  password: "urlshortener_pass"
  name: "urlshortener"

redis:
  host: "redis"
  port: "6379"
  password: ""

jwt:
  secret: "dev-jwt-secret-change-in-production"

webauthn:
  rp_id: "localhost"
  rp_origin: "http://localhost:3000"

urls:
  base_url: "http://localhost:8080"

geoip:
  path: "/app/data/GeoLite2-City.mmdb"
